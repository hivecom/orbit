{
  "name": "connector",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "vp run wasm && vue-tsc -b",
    "test": "vp test",
    "wasm": "wasm-pack build ../core-wasm --target web",
    "dev": "cargo watch -i pkg -C ../core-wasm -s 'wasm-pack build ../core-wasm --target web'"
  },
  "dependencies": {
    "core-wasm": "workspace:*",
    "vue": "^3.5.34"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "@vue/tsconfig": "^0.9.1",
    "jsdom": "catalog:",
    "typescript": "~6.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vue-tsc": "^3.2.8"
  }
}
