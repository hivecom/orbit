{
  "name": "orbit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vp run -r test",
    "ready": "vp check && vp run -r test && vp run -r build",
    "prepare": "vp config",
    "dev": "vp run web#dev",
    "dev:core": "vp run web#dev:core",
    "dev:desktop": "vp run desktop#dev",
    "build": "vp run web#build",
    "build:desktop": "vp run desktop#build"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.5.3"
}
